server:
  port: 9090
  host: "0.0.0.0"
  mode: release

redis:
  host: "redis"
  port: 6379
  password: ""
  db: 0

minio:
  endpoint: "minio:9000"
  accesskey: "GoNetDisk"
  secretkey: "GoNetDisk"
  bucket: "GoNetDisk"
  usessl: false

database:
  host: "postgres"
  port: 5432
  user: "GoNetDisk"
  password: "GoNetDisk"
  name: "GoNetDisk"

jwt:
  secret: "请修改为随机字符串"
  accessexpiresmin: 18
  refreshexpireshour: 168

upload:
  maxFileSizeMB: 100
